{
  "name": "tucker-pippin-website",
  "version": "1.0.0",
  "private": true,
  "description": "Tucker Pippin personal website with CMS",
  "scripts": {
    "build": "node scripts/optimize-images.js && node generate-index.js",
    "dev": "netlify dev",
    "test": "node --test tests/*.test.mjs",
    "migrate:tracker": "node scripts/migrate-tracker-data.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "10.1.0",
    "@netlify/functions": "^5.3.0",
    "@netlify/identity": "^1.2.0",
    "heic-convert": "2.1.0",
    "sharp": "^0.33.5"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "overrides": {
    "@netlify/blobs": "10.1.0"
  },
  "devDependencies": {
    "netlify-cli": "23.12.0"
  }
}
